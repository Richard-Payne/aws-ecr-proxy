events {
  worker_connections 4096;
}

http {
  resolver DNS_RESOLVERS;

  server {

    listen 80 default_server;
    server_name _;

    location / {
      set $upstream           REGISTRY_URL;
      set $token1             $ecr_token1;
      set $token2             $ecr_token2;

      proxy_pass              $upstream;
      proxy_redirect          $upstream https://$host;

      proxy_set_header        X-Real-IP            $remote_addr;
      proxy_set_header        X-Forwarded-For      $remote_addr;
      proxy_set_header        X-Forwarded-User     "Basic $token1$token2";
      proxy_set_header        Authorization        "Basic $token1$token2";

      proxy_pass_header       Server;

      client_max_body_size    0;
      proxy_connect_timeout   300s;
      proxy_read_timeout      300s;
      proxy_send_timeout      300s;
      send_timeout            300s;
    }
  }

  server {
    listen 443 default_server;
    server_name _;
    ssl on;
    ssl_certificate           /etc/nginx/ssl/default.crt;
    ssl_certificate_key       /etc/nginx/ssl/default.key;

    location / {
      set $upstream           REGISTRY_URL;
      set $token1             $ecr_token1;
      set $token2             $ecr_token2;

      proxy_pass              $upstream;
      proxy_redirect          $upstream https://$host;

      proxy_set_header        X-Real-IP            $remote_addr;
      proxy_set_header        X-Forwarded-For      $remote_addr;
      proxy_set_header        X-Forwarded-User     "Basic $token1$token2";
      proxy_set_header        Authorization        "Basic $token1$token2";

      proxy_pass_header       Server;

      client_max_body_size    0;
      proxy_connect_timeout   300s;
      proxy_read_timeout      300s;
      proxy_send_timeout      300s;
      send_timeout            300s;
    }
  }
}
